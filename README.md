# AI Blog Agent (standalone)

This is a standalone AI agent that fetches a trending topic and generates a Markdown blog post (saved into `blogs/`).

## Quick start (local)
1. Create Python venv and activate:
   ```bash
   python -m venv .venv
   source .venv/bin/activate    # Windows: .venv\Scripts\activate
